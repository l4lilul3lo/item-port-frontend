// const { execSync } = require("child_process");
// if (process.env.NODE_ENV === "local_network") {
//   process.env.WSL_IP = execSync("hostname -I").toString().trim();
// }

console.log(process.env);
